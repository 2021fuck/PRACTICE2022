#include <bits\stdc++.h>
using namespace std;
int fun1(int x, int y) {
  int ans;
  return ans;
}
int fun2(int x, int y) {
  int ans;
  return ans;
}
int main() {
  int n, x, y;
  cin >> n;
  while (n--) {
    cin >> x >> y;
    cout << fun1(x, y) << fun2(x, y);
  }
  system("pause");
  return 0;
}