// #include <cmath>
#include <iostream>
using namespace std;
int main() {
  // long long ans[61], N;
  // int num;
  // for (int i = 0; i < 61; i++) ans[i] = pow(2, i);
  // cin >> N;
  // for (int i = 0; i < 61; i++) {
  //   if (ans[i] <= N && ans[i + 1] > N) {
  //     num = i;
  //     break;
  //   }
  // }
  // cout << num;
  // // system("pause");
  // cout<<"傻逼";
  printf("傻逼");
  system("pause");
  return 0;
}
